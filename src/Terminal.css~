.Terminal {
    position: absolute;
    height: 400px;
    width: 600px;
    border-radius: 10px;
    display: flex;
    flex-direction: column;
    align-items: stretch;
    overflow: hidden;
}

.lines-container {
    height: inherit;
    width: inherit;
    padding: 0.2rem;
    background-color: rgba(0, 0, 0, 0.9);
    color: white;
    font-family: "Ubuntu Mono", "Roboto Mono", sans-serif;
    font-style: normal;
    font-variant: normal;
    font-size: 22px;
    font-weight: 400;
    white-space: pre-wrap;
    word-break: break-all;
    overflow-y: auto;
}

.directory {
    font-weight: bold;
    color: #2AA198;
}

.file {
    font-weight: normal;
    color: #E885E7;
}

.prompt {
    font-weight: bold;
    color: #fda51d;
}

.input-text {
    font-weight: normal;
    color: #E885E7;
}

